\* TLC configuration for the Effects skeleton (verify/exercise.md, Phase 4).
SPECIFICATION Spec
CONSTANTS
  Oids = {o1, o2, o3}
  RefNames = {r1, r2}
  EffectRefs = {r2}
  Keys = {k1, k2}
  NoOid = NoOid
INVARIANT TypeOK
